# AI小商 - 开发规范

> 本文档定义代码规范、错误处理、日志记录、API设计等核心规则。所有开发工作必须遵循本规范。

---

## 一、技术栈

| 层级 | 技术 | 说明 |
|-----|-----|-----|
| 前端 | Vue.js 3 + Element Plus + Vant | 响应式Web应用 |
| 后端 | Python FastAPI + SQLAlchemy | 异步高性能框架 |
| 数据库 | MySQL 8.0 | 关系型数据存储 |
| 向量库 | ChromaDB | RAG知识库向量检索 |
| AI能力 | 讯飞开放平台 | 星火+ASR+TTS+OCR+NLP |

> **注意**：本项目为比赛演示用，不使用Redis，不使用微服务架构，保持轻量简单。

---

## 二、目录结构

```
backend/app/
├── __init__.py
├── main.py              # 应用入口，注册中间件和路由
├── config.py            # 配置管理（所有配置从此处导入）
├── database.py         # 数据库连接
│
├── models/             # SQLAlchemy 模型（按模块划分）
│   ├── __init__.py
│   ├── user.py         # 用户相关模型
│   ├── course.py       # 课表相关模型
│   ├── schedule.py     # 日程相关模型
│   ├── review.py       # 录音回顾相关模型
│   ├── chat.py         # 对话相关模型
│   └── admin.py        # 管理端相关模型
│
├── schemas/            # Pydantic 请求/响应模型
│   ├── __init__.py
│   ├── auth.py
│   ├── chat.py
│   ├── timetable.py
│   └── ...
│
├── routers/            # API 路由（按模块划分）
│   ├── __init__.py
│   ├── auth.py
│   ├── chat.py
│   ├── timetable.py
│   ├── review.py
│   ├── export.py
│   └── admin.py
│
├── services/           # 业务逻辑层
│   ├── __init__.py
│   ├── xinghuo_service.py    # 星火大模型
│   ├── asr_service.py        # 语音识别
│   ├── tts_service.py        # 语音合成
│   ├── ocr_service.py        # 文字识别
│   ├── nlp_service.py        # NLP情感分析
│   ├── knowledge_base.py     # RAG知识库
│   ├── docx_generator.py     # DOCX生成
│   └── pptx_generator.py     # PPTX生成
│
└── utils/              # 工具函数
    ├── __init__.py
    ├── errors.py       # 统一错误处理
    ├── logging.py      # 日志配置
    └── prompt_builder.py  # 提示词构建
```

---

## 三、错误处理规范

### 3.1 统一错误类

```python
# backend/app/utils/errors.py

from fastapi import HTTPException
from typing import Optional, Any
import traceback
import logging

logger = logging.getLogger(__name__)

class AppException(Exception):
    """应用级异常基类"""
    def __init__(
        self,
        message: str,
        code: str = "APP_ERROR",
        status_code: int = 500,
        details: Optional[Any] = None
    ):
        self.message = message
        self.code = code
        self.status_code = status_code
        self.details = details
        super().__init__(self.message)

    def to_dict(self):
        return {
            "success": False,
            "error": {
                "code": self.code,
                "message": self.message,
                "details": self.details
            }
        }

class NotFoundException(AppException):
    """资源不存在"""
    def __init__(self, resource: str, identifier: Any):
        super().__init__(
            message=f"{resource}不存在: {identifier}",
            code="NOT_FOUND",
            status_code=404
        )

class ValidationException(AppException):
    """数据验证失败"""
    def __init__(self, message: str, details: Any = None):
        super().__init__(
            message=message,
            code="VALIDATION_ERROR",
            status_code=422,
            details=details
        )

class ThirdPartyException(AppException):
    """第三方服务异常"""
    def __init__(self, service: str, message: str, original_error: Exception = None):
        logger.error(f"[{service}] {message}", exc_info=original_error)
        super().__init__(
            message=f"{service}暂时不可用，请稍后重试",
            code="THIRD_PARTY_ERROR",
            status_code=502,
            details={"service": service}
        )

class AuthenticationException(AppException):
    """认证失败"""
    def __init__(self, message: str = "认证失败"):
        super().__init__(
            message=message,
            code="AUTH_ERROR",
            status_code=401
        )
```

### 3.2 全局异常处理器

```python
# backend/app/main.py

from fastapi import FastAPI, Request
from fastapi.responses import JSONResponse
from app.utils.errors import AppException
from app.utils.logging import setup_logging

# 初始化日志
setup_logging()
logger = logging.getLogger(__name__)

app = FastAPI(title="AI小商", version="1.0.0")

@app.exception_handler(AppException)
async def app_exception_handler(request: Request, exc: AppException):
    """处理应用异常"""
    logger.warning(f"[{exc.code}] {exc.message}")
    return JSONResponse(status_code=exc.status_code, content=exc.to_dict())

@app.exception_handler(Exception)
async def general_exception_handler(request: Request, exc: Exception):
    """处理未捕获的异常"""
    logger.error(f"[UNHANDLED] {str(exc)}", exc_info=True)
    return JSONResponse(
        status_code=500,
        content={
            "success": False,
            "error": {
                "code": "INTERNAL_ERROR",
                "message": "服务器内部错误，请稍后重试"
            }
        }
    )
```

### 3.3 讯飞API重试机制

```python
# backend/app/services/xinghuo_service.py

import asyncio
import logging
from typing import List, Dict

logger = logging.getLogger("xfyun")

class XingHuoService:
    MAX_RETRIES = 3
    RETRY_DELAY = 1.0

    async def chat_with_retry(self, messages: List[Dict]) -> str:
        """带重试的星火调用"""
        last_error = None
        for attempt in range(self.MAX_RETRIES):
            try:
                result = await self._do_chat(messages)
                logger.info(f"[XingHuo] Success after {attempt + 1} attempts")
                return result
            except Exception as e:
                last_error = e
                logger.warning(f"[XingHuo] Attempt {attempt + 1} failed: {e}")
                if attempt < self.MAX_RETRIES - 1:
                    await asyncio.sleep(self.RETRY_DELAY * (attempt + 1))

        raise ThirdPartyException(
            service="XingHuo",
            message="星火大模型调用失败",
            original_error=last_error
        )
```

---

## 四、日志规范

### 4.1 日志配置

```python
# backend/app/utils/logging.py

import logging
import sys
from pathlib import Path
from logging.handlers import RotatingFileHandler

LOG_DIR = Path("logs")
LOG_DIR.mkdir(exist_ok=True)

LOG_FORMAT = "%(asctime)s [%(levelname)8s] [%(name)s] %(message)s"
DATE_FORMAT = "%Y-%m-%d %H:%M:%S"

def setup_logging():
    """配置全局日志"""
    root_logger = logging.getLogger()
    root_logger.setLevel(logging.INFO)
    root_logger.handlers.clear()

    # 控制台
    console = logging.StreamHandler(sys.stdout)
    console.setFormatter(logging.Formatter(LOG_FORMAT, DATE_FORMAT))
    root_logger.addHandler(console)

    # 文件
    file_handler = RotatingFileHandler(
        LOG_DIR / "app.log",
        maxBytes=10*1024*1024,
        backupCount=5,
        encoding="utf-8"
    )
    file_handler.setFormatter(logging.Formatter(LOG_FORMAT, DATE_FORMAT))
    root_logger.addHandler(file_handler)

def get_logger(name: str) -> logging.Logger:
    """获取指定名称的Logger"""
    return logging.getLogger(name)
```

### 4.2 日志使用

```python
logger = logging.getLogger("api")  # API请求日志
xfyun_logger = logging.getLogger("xfyun")  # 讯飞API日志
db_logger = logging.getLogger("database")  # 数据库日志

# 使用示例
logger.info(f"[{request_id}] GET /api/chat from {client_ip}")
xfyun_logger.warning(f"[ASR] Retry {attempt}/{MAX_RETRIES}")
db_logger.error(f"[DB] Query timeout after 30s")
```

---

## 五、API 设计规范

### 5.1 响应格式

```python
# 成功
{
    "success": True,
    "data": { ... },
    "message": "操作成功"
}

# 错误
{
    "success": False,
    "error": {
        "code": "NOT_FOUND",
        "message": "资源不存在",
        "details": { ... }
    }
}
```

### 5.2 命名规范

| 操作 | 方法 | URL |
|-----|-----|-----|
| 获取列表 | GET | `/api/{resource}` |
| 获取单个 | GET | `/api/{resource}/{id}` |
| 创建 | POST | `/api/{resource}` |
| 更新 | PUT | `/api/{resource}/{id}` |
| 删除 | DELETE | `/api/{resource}/{id}` |
| 批量 | POST | `/api/{resource}/batch` |

---

## 六、数据库规范

### 6.1 命名规范

- 表名：英文小写 + 下划线，如 `courses`, `student_profiles`
- 字段名：英文小写 + 下划线，如 `created_at`, `is_active`
- 外键：`{table}_id`，如 `user_id`, `course_id`

### 6.2 公共字段

```python
class BaseModel:
    id = Column(Integer, primary_key=True, autoincrement=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    is_deleted = Column(Boolean, default=False)  # 软删除
```

### 6.3 索引规范

- 主键、外键必须有索引
- 频繁组合查询的字段建复合索引
- 避免过多索引

---

## 七、环境变量

```bash
# .env.example

# 数据库
DATABASE_URL=mysql+aiomysql://user:pass@localhost:3306/ai_xiaoshang

# 讯飞
XFYUN_APP_ID=your_app_id
XFYUN_API_KEY=your_api_key
XFYUN_API_SECRET=your_api_secret

# JWT
SECRET_KEY=your-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# 上传
UPLOAD_DIR=uploads
MAX_AUDIO_SIZE_MB=50
MAX_IMAGE_SIZE_MB=5
```

---

## 八、Git 提交规范

```
<type>(<scope>): <subject>

feat(timetable): 添加课表OCR导入
fix(chat): 修复语音输入白屏问题
docs(api): 更新接口文档
refactor(xinghuo): 统一错误处理
```
