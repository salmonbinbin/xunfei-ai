# AI小商 - 数据库设计

> MySQL 8.0 数据库详细设计。完整建表脚本运行 `backend/scripts/init_db.sql`。

---

## 一、ER图

```
┌────────────┐       ┌────────────────┐       ┌────────────┐
│   User     │       │ StudentProfile │       │   Course   │
│  (用户)    │◄──1:1──│   (学生画像)    │◄──1:N──│   (课程)    │
└────────────┘       └────────────────┘       └────────────┘
       │                                            │
       │ 1:N                                        │
       ▼                                            │
┌────────────┐       ┌────────────────┐             │
│ Schedule   │       │ ReviewRecord   │             │
│  (日程)    │       │ (录音回顾)      │             │
└────────────┘       └───────┬────────┘             │
                             │ 1:N                   │
                             ▼                       │
                     ┌───────────────┐              │
                     │ Transcription │              │
                     │   (转写)      │              │
                     └───────┬───────┘              │
                             │ 1:1                   │
                             ▼                       │
                     ┌───────────────┐              │
                     │   Summary     │              │
                     │   (总结)      │              │
                     └───────────────┘              │
                                                     │
┌────────────┐       ┌────────────────┐             │
│Conversation│◄──1:N──│    Message     │             │
│  (对话会话) │       │   (消息记录)   │             │
└────────────┘       └────────────────┘             │
                             │
┌────────────┐       ┌────────────────┐
│KnowledgeBase│      │ AdminUser     │
│   (知识库)   │      │  (管理员)      │
└────────────┘       └────────────────┘
                             │
┌────────────┐       ┌────────────────┐
│TranslationTask│      │ AdminUser     │
│   (翻译任务)  │      │  (管理员)      │
└────────────┘       └────────────────┘
```

---

## 二、表结构

### 2.1 users（用户表）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| openid | VARCHAR(64) | UNIQUE, NOT NULL | 微信openid |
| unionid | VARCHAR(64) | | 微信unionid |
| nickname | VARCHAR(64) | | 昵称 |
| phone | VARCHAR(20) | | 手机号 |
| avatar_url | VARCHAR(500) | | 头像URL |
| is_active | TINYINT(1) | DEFAULT 1 | 账号状态 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE | 更新时间 |
| last_login | DATETIME | | 最后登录 |

### 2.2 translation_tasks（翻译任务表）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| task_id | VARCHAR(64) | UNIQUE, NOT NULL | 任务ID（如trans_123456） |
| user_id | INT | FK | 关联users |
| task_type | VARCHAR(20) | NOT NULL | text/document |
| source_lang | VARCHAR(10) | | 源语言 |
| target_lang | VARCHAR(10) | NOT NULL | 目标语言 |
| original_content | LONGTEXT | | 原文内容（即时翻译） |
| original_file_url | VARCHAR(500) | | 原文件URL（文档翻译） |
| translated_content | LONGTEXT | | 翻译结果 |
| word_count | INT | | 词数统计 |
| status | VARCHAR(20) | DEFAULT 'pending' | pending/processing/completed/failed |
| error_message | VARCHAR(500) | | 错误信息 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE | 更新时间 |

### 2.2 student_profiles（学生画像）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| user_id | INT | UNIQUE, FK | 关联users |
| major | VARCHAR(100) | | 专业 |
| grade | TINYINT | | 年级1-4 |
| class_name | VARCHAR(50) | | 班级 |
| goal | VARCHAR(20) | | 考研/考公/就业/出国/未定 |
| goal_detail | TEXT | | 目标详细描述 |
| voice_prefer | TINYINT(1) | DEFAULT 1 | 偏好语音 |
| emotion_mode | VARCHAR(20) | DEFAULT 'normal' | normal/emotion |
| gpa | DECIMAL(3,2) | | 绩点 |
| completed_courses | JSON | | 已修课程 |
| updated_at | DATETIME | ON UPDATE | 更新时间 |

### 2.3 courses（课程表）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| user_id | INT | FK | 关联users |
| name | VARCHAR(100) | NOT NULL | 课程名 |
| code | VARCHAR(20) | | 课程代码 |
| credit | DECIMAL(3,1) | | 学分 |
| category | VARCHAR(20) | | 通识/专业/选修 |
| day_of_week | TINYINT | NOT NULL | 周几1-7 |
| start_slot | TINYINT | NOT NULL | 第几节 |
| end_slot | TINYINT | NOT NULL | 第几节 |
| week_range | VARCHAR(20) | DEFAULT '1-16周' | 周次 |
| location | VARCHAR(100) | | 上课地点 |
| teacher | VARCHAR(50) | | 教师 |
| ai_tip | VARCHAR(200) | | AI学习建议 |
| is_active | TINYINT(1) | DEFAULT 1 | 有效 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.4 schedules（日程表）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| user_id | INT | FK | 关联users |
| event | VARCHAR(200) | NOT NULL | 日程内容 |
| event_type | VARCHAR(20) | DEFAULT '日程' | 类型 |
| day_of_week | TINYINT | | 周几 |
| time_desc | VARCHAR(50) | | 时间描述 |
| location | VARCHAR(100) | | 地点 |
| is_completed | TINYINT(1) | DEFAULT 0 | 已完成 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.5 review_records（录音回顾记录）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| user_id | INT | FK | 关联users |
| record_type | VARCHAR(10) | NOT NULL | 课程/会议 |
| title | VARCHAR(200) | | 标题 |
| audio_url | VARCHAR(500) | | 音频URL |
| duration | INT | | 时长(秒) |
| language | VARCHAR(20) | DEFAULT 'mandarin' | 语言 |
| status | VARCHAR(20) | DEFAULT 'pending' | pending/processing/completed/failed |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE | 更新时间 |

### 2.6 transcriptions（转写结果）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| record_id | INT | FK, UNIQUE | 关联review_records |
| raw_text | LONGTEXT | | 原始转写文字 |
| segments | JSON | | 分段结果 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.7 summaries（总结结果）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| record_id | INT | FK, UNIQUE | 关联review_records |
| topic | VARCHAR(200) | | 主题 |
| key_points | TEXT | | 核心知识点 |
| difficulties | TEXT | | 重点难点 |
| memorable_quote | VARCHAR(500) | | 金句 |
| next_suggestion | TEXT | | 预习建议 |
| full_text | TEXT | | 完整总结 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.8 conversations（对话会话）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| user_id | INT | FK | 关联users |
| title | VARCHAR(200) | | 会话标题 |
| category | VARCHAR(20) | DEFAULT 'general' | 分类 |
| mode | VARCHAR(20) | DEFAULT 'normal' | normal/emotion |
| context_summary | TEXT | | 上下文摘要 |
| message_count | INT | DEFAULT 0 | 消息数 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE | 更新时间 |

### 2.9 messages（消息记录）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| conv_id | INT | FK | 关联conversations |
| role | VARCHAR(10) | NOT NULL | user/assistant |
| content_type | VARCHAR(20) | DEFAULT 'text' | text/voice |
| content | TEXT | | 消息内容 |
| audio_url | VARCHAR(500) | | 语音URL |
| sentiment | VARCHAR(20) | | 情感分析结果 |
| tokens_used | INT | | token消耗 |
| latency_ms | INT | | 响应延迟 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

### 2.10 knowledge_base（知识库）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| category | VARCHAR(50) | NOT NULL | 分类 |
| question | VARCHAR(500) | | 问题 |
| answer | TEXT | | 回答 |
| content | TEXT | | 原始内容 |
| content_vector | VECTOR(1536) | | 向量 |
| source | VARCHAR(200) | | 来源 |
| is_active | TINYINT(1) | DEFAULT 1 | 有效 |
| views | INT | DEFAULT 0 | 查询次数 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | ON UPDATE | 更新时间 |

### 2.11 admin_users（管理员）

| 字段 | 类型 | 约束 | 说明 |
|-----|-----|-----|-----|
| id | INT | PK | 主键 |
| username | VARCHAR(50) | UNIQUE | 用户名 |
| password_hash | VARCHAR(255) | | 密码 |
| role | VARCHAR(20) | DEFAULT 'admin' | 角色 |
| nickname | VARCHAR(50) | | 昵称 |
| last_login | DATETIME | | 最后登录 |
| is_active | TINYINT(1) | DEFAULT 1 | 有效 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |

---

## 三、索引设计

```sql
-- users
INDEX idx_openid (openid)
INDEX idx_phone (phone)

-- translation_tasks
INDEX idx_task_id (task_id)
INDEX idx_user_status (user_id, status)

-- courses
INDEX idx_user_day (user_id, day_of_week)

-- schedules
INDEX idx_user_today (user_id, day_of_week, is_completed)

-- review_records
INDEX idx_user_status (user_id, status)

-- conversations
INDEX idx_user_updated (user_id, updated_at)

-- messages
INDEX idx_conv_time (conv_id, created_at)

-- knowledge_base
INDEX idx_category (category)
INDEX idx_active (is_active)
```

---

## 四、ChromaDB（RAG知识库）

```python
# backend/app/services/knowledge_base.py

import chromadb
from chromadb.config import Settings

class KnowledgeBase:
    def __init__(self):
        self.client = chromadb.Client(Settings(
            persist_directory="data/chroma_db",
            anonymized_telemetry=False
        ))
        self.collection = self.client.get_or_create_collection(
            name="knowledge_base",
            metadata={"description": "AI小商校园知识库"}
        )

    def add(self, id: str, text: str, metadata: dict):
        """添加知识条目"""
        self.collection.add(
            ids=[id],
            documents=[text],
            metadatas=[metadata]
        )

    def search(self, query: str, top_k: int = 5) -> list:
        """语义检索"""
        results = self.collection.query(
            query_texts=[query],
            n_results=top_k
        )
        return results["documents"][0]

    def delete(self, id: str):
        """删除知识条目"""
        self.collection.delete(ids=[id])
```

---

## 五、初始化脚本

```sql
-- backend/scripts/init_db.sql
-- 运行: mysql -u root -p < init_db.sql

CREATE DATABASE IF NOT EXISTS ai_xiaoshang
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

USE ai_xiaoshang;

-- 创建所有表（DDL见上方）

-- 初始化FAQ
INSERT INTO knowledge_base (category, question, answer) VALUES
('faq', '图书馆开放时间？', '周一至周五 8:00-22:00，周六周日 9:00-21:00'),
('faq', '如何申请调课？', '联系任课教师说明原因，填写调课申请表，由教务处审批'),
('faq', '9教是指哪栋楼？', '9教是9号教学楼，位于学校东门附近，主要用于经济管理学院教学');
```
